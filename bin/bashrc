#!/bin/bash
# Setup project context

export p=$HOME/Projects/Hammer

alias bl='cd $p/bin && ls *.py'
alias ce='c cmd edit'
alias cl='c cmd list'
alias console='x server console'
alias de='c doc edit'
alias dl='c doc list'
alias deploy='x server deploy'
alias hammer=". $p/bin/bashrc"
alias restart='x server restart'
alias todo='c todo'
alias tl='c tool list'
alias te='c tool edit'
alias web='x server web'

function be {
    e $p/bin/${1/.py/}.py
}

cat $p/bin/welcome

# Path variable
export PATH=\
$p/bin:\
/bin:\
/sbin:\
/usr/bin:\
/usr/local/bin:\
/opt/local/bin

echo $PATH
export _OLD_VIRTUAL_PATH=$PATH


[ -f ~/Tools/env-python27/bin/activate ] &&
. ~/Tools/env-python27/bin/activate
echo "Active python = `which python`"
echo

cd $p

